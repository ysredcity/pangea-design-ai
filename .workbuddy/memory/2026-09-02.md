# 2026-09-02

## 同步最新构建到线上（静态部署流程复用）
- 用户说「重新补充一下」：本地 dist 今早 07:19 有新构建（`index-hiQ0R_xy.js`），线上还是昨晚的 `DaT-PRzr`。
- 流程：刷新 `agent-layout-static` 目录（复制最新 dist/*）→ `language:"static"` 重新部署 → curl 验证线上哈希与本地一致（hiQ0R_xy / DJ120LLV）✅
- 链接不变：https://be982a4f59b744aba053ade680a5df07.app.workbuddy.link
