# Immersive Base UI adapter boundary

The immersive template keeps its proven AppConfig, panel registry, and image-viewer adapter local during the first extraction. This directory defines the stable package subpath and neutral shared contracts; it intentionally does not let panel types cross into `conversation/`.
