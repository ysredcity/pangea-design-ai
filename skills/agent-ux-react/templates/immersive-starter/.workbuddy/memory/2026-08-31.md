# 2026-08-31

## 部署 agent-layout 到线上
- 项目：agent-layout（Vite 8 + React 19 + TS + Tailwind v4，Agent 布局界面）。
- 修改 `vite.config.ts`：新增 `server.host='0.0.0.0'`、`server.allowedHosts=true`、`preview.host='0.0.0.0'`、`preview.allowedHosts=true`，供反向代理访问。
- 部署要点（Vite 项目）：自动探测会用 `vite --host 0.0.0.0`（dev，默认 5173），导致 3000 探测失败；`$PORT` 在 startCmd 里展开为空。最终用固定端口：`npm run build && npm run preview -- --host 0.0.0.0 --port 3000`，port=3000。
- 分享链接：https://9a70e2491c254bd3bfd59b42db503110.app.workbuddy.link
- 22:07 代码变动后重新部署，链接不变，startCmd 沿用 `npm run build && npm run preview -- --host 0.0.0.0 --port 3000`，一次成功。
