# 2026-09-02

## 同步最新构建到线上（静态部署流程复用）
- 用户说「重新补充一下」：本地 dist 今早 07:19 有新构建（`index-hiQ0R_xy.js`），线上还是昨晚的 `DaT-PRzr`。
- 流程：刷新 `agent-layout-static` 目录（复制最新 dist/*）→ `language:"static"` 重新部署 → curl 验证线上哈希与本地一致（hiQ0R_xy / DJ120LLV）✅
- 链接不变：https://be982a4f59b744aba053ade680a5df07.app.workbuddy.link

## 18:13 重新部署（本地 tsc 被 OOM 杀）
- 项目已扩展（src 43→54 文件，node_modules 23460）。`npm run build`（含 `tsc -b`）被系统 SIGKILL（OOM，Killed: 9），单独 `tsc` 内存不足。
- 解法：跳过 tsc，直接 `./node_modules/.bin/vite build`（3357 modules，产物 `index-BP68V_NO.js` 807KB）成功。
- 注意：跳过 tsc 意味着本次部署未做类型检查，运行时不受影响；后续要过类型检查需在有内存余量时单独跑 tsc。
- 发布产物 BP68V_NO / DLZ6V3ZU 已上线，链接不变。
