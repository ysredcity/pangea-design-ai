# Website（预留）

该目录曾承载仓库内部的文档站、模板演示、组件图谱和 JSON 剧本编辑器。为降低维护复杂度，现有实现已于 2026-09-06 退役。

当前约定：

- 本目录仅作为未来网站工作的路径占位，不属于 npm workspace。
- 不进入根级 `npm run gate`，也不随 `pangea-design-ai` skill 发布。
- 后续如重启网站开发，应先重新确认产品目标、信息架构和与 `packages/agent-ui` 的消费边界，不直接恢复旧实现。

历史方案与实施记录保留在 `docs/proposals/` 和 `docs/plans/`，仅用于追溯，不代表当前工程状态。
